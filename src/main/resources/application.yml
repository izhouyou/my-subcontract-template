spring:
  profiles:
    active: local
server:
  port: 8080
swagger:
  base-package: com.izhouy.template
  title: ${spring.application.name}
  author-name: jtlsp
logback:
  path: .logs
  service-name: ${spring.application.name}
logging:
  config: classpath:logback/logback-spring.xml